import java.time.LocalDate;

public class Lion extends Animal {
    public Lion(String name, float weight, LocalDate age, Owner owner, boolean canFly, boolean canSwim) {
        super(name, weight, age, owner, canFly, canSwim);
    }
}
